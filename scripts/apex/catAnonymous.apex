// normal world

// 1. if our class has no constructor defined.
// we can create new instance with empty brackets.
Cat sfcat = new Cat();
sfcat.name = 'tekir';
sfcat.color = 'black';

cat c2 = new cat('milky', 'white');
System.debug(c2);