public class PrimitiveData_Assignment {
    public void prt() {
        Id idNum = '001Dn00000Aok4TIAR';
        String acc = 'the following Id belongs to Account Burlington Textiles Corp of America';
        Boolean check = true;
        system.debug(idNum + ' ' + acc + ' ' + check); 
    }
}